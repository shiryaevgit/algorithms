```variant#1
func missingNumber(nums []int) int {
	n := len(nums)
	overSum := (n * (n + 1)) / 2

	for _, num := range nums {
		overSum -= num
	}

	return overSum 
}
```

Оценка по времени: O(n), где n - количество элементов
Объяснения:

В худшем:
В лучшем: 

Оценка по памяти:  
O(1)


Объяснение решения:
1. вычисляем общую сумму 
2. в цикле вычитаем все элементы массива из overSum
3. возвращаем остаток overSum

```variant#2

```